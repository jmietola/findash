# findash
Financial Dashboard

Example template to be used with QCore API(QC-API unifies several banking APIs to one source). 
One can monitor all assets at one glance and do transactions between different vendors.  

User interface(Financial Dashboard) uses following technologies Bootstrap 4, sass, gulp, chart.js and it's public. 
At the moment QCore is private.
